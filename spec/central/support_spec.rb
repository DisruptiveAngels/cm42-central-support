require 'spec_helper'

describe Central::Support do
  it 'has a version number' do
    expect(Central::Support::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
